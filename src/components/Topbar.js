import React from "react";

export default function () {
  return (
    <nav className="flex w-screen items-center justify-between flex-wrap bg-teal-500 p-6">
      <div className="flex items-center flex-shrink-0 text-white mr-6">
        <span className="font-semibold text-xl tracking-tight">Web Cat</span>
      </div>
    </nav>
  );
}
